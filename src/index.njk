---
title: bitstorm
layout: 'base.njk'
---

<h1>Welcome!</h1>
<p>This is my personal website where I store my projects and sometimes write blog posts. I mainly do web development and a bit of game development using the <a href="https://unity.com">Unity Game Engine</a>. Check them out!</p>

<h2>Latest Posts</h2>


<!-- Should only show last 5-10 posts -->
<ol>
    {%- for post in collections.post-%}
    <li>
        <a href='{{ post.url }}'>{{ post.data.title }}</a>
        <!-- <p>{{ post.data.time }}</p> -->
        <p>{{ post.data.description }}</p>
        <img class="preview" src="{{ post.data.image }}" alt="{{ post.data.imageAlt}}">
    </li>
    {%- endfor %}
</ol>

<p>More posts can be found in the <a href="/blog">blog</a> section.</p>